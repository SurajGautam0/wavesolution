# wave_solution
 This is cleaning project

## AI outreach draft generator

The admin dashboard includes a manual outreach draft generator powered by Groq's API.

Required environment variable:

- `GROQ_API_KEY=your_groq_api_key`

The dashboard uses the API route `/api/admin/ai-outreach` to generate short outreach drafts for manual review.
